PORT=3141
DB_PATH=./data.sqlite
TRUSTED_ORIGINS=http://localhost:5173
BETTER_AUTH_SECRET=change-me-in-production
AUTH_BASE_URL=http://localhost:3141

# Optional — omit to run anonymous-only auth (see ticket 05):
# LOGTO_ENDPOINT=
# LOGTO_APP_ID=
# LOGTO_APP_SECRET=
# LOGTO_REDIRECT_URI=

# Optional — GlitchTip DSN; error reporting is off when unset:
# GLITCHTIP_DSN=
